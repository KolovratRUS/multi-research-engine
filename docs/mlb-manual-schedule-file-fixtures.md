# MLB Manual Schedule-File Fixtures

Local-only static fixtures and golden validation outputs.
No live source used.
No real MLB API request made.
No web lookup used.
No real schedule ingestion performed.
No parser CLI implemented.
No generated prospective run artifact committed.
No historical fixture data added or modified.
No model-quality or predictive-performance claim.
modelProbability remains null/absent/not available until calibrated.

## Purpose

Phase 4H adds tiny static manual schedule fixtures and expected validator outputs before any CLI/parser.
This phase locks deterministic validation behavior and is entirely local-only.

## References

- `docs/mlb-manual-schedule-file-dry-run-plan.md`
- `docs/mlb-manual-schedule-file-schemas.md`
- `docs/mlb-prospective-weekly-test-mode-plan.md`

## Fixture files

- `tests/prospective/fixtures/manual-schedule/valid-manual-schedule-v1.json`
- `tests/prospective/fixtures/manual-schedule/valid-manual-schedule-validation-v1.json`
- `tests/prospective/fixtures/manual-schedule/invalid-forbidden-fields-v1.json`
- `tests/prospective/fixtures/manual-schedule/invalid-forbidden-fields-validation-v1.json`

## Valid fixture

- 2 fake/local-looking games
- `sourceMode`: `manual-schedule`
- no final scores/results/completed state
- no actual starters
- no external price/market-like fields

## Invalid fixture

- deliberately includes forbidden pre-game fields in a game
- deliberately includes forbidden external field names in a game
- golden output locks expected validation errors

## Safety boundary

- fixtures are static local test files
- no files are generated by the command
- no live/API/web
- no real schedule ingestion
- no historical fixture mutation
- converted snapshots exclude `finalScore`/`completedGameState`
- `modelProbability` remains null/absent/not available

## Validation

- `npx vitest run tests/prospective/mlb-manual-schedule-file.test.ts --reporter=verbose` passes with fixture/golden tests included.
- `npx tsc --noEmit --incremental false --pretty false` passes.
- Full test suite passes: `npm test`.
- Build passes: `npm run build`.
- Git diff check passes: `git diff --check`.
- Inventory guard still passes: `npm run inventory:mlb-fixtures` reports 29 total games (2024-06-01 through 2024-07-21, June 17, July 12).

## Recommended next safe phase

Phase 4I — add manual schedule validator CLI.
State:
- local-only
- no live/API/web
- reads only a user-provided local JSON path
- validates only
- no snapshot creation yet
- no generated run artifacts committed
- no historical fixture data changes

See `docs/mlb-manual-schedule-validator-cli.md` for the validator CLI documentation.
